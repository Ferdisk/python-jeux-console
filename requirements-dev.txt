# Outils de la chaîne d'intégration continue.
# Chaque outil correspond à une étape attendue par le cours R5.A.07.
#
# Les versions sont épinglées pour que le pipeline soit reproductible.
# pip-audit vérifie à chaque exécution qu'aucune n'est vulnérable, et
# Dependabot ouvre une pull request dès qu'une correction paraît.

# Tests unitaires
pytest==9.1.1

# Couverture de code
pytest-cov==7.1.0

# Analyse statique et style (comparatif du rapport R5.A.07)
pylint==4.0.8
flake8==7.3.0
ruff==0.16.6
black==26.5.1

# Sécurité : analyse du code (SAST) et des dépendances
bandit==1.9.4
pip-audit==2.10.1
